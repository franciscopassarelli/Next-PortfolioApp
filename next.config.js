/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['reqres.in', 'avatars.githubusercontent.com', 'encrypted-tbn0.gstatic.com', 'w7.pngwing.com'],
  },
};

module.exports = nextConfig;
